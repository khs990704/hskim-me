{
  "name": "hskim-me",
  "version": "0.1.0",
  "private": true,
  "description": "hskim.me — 저장소 루트. 실제 코드는 pipeline/ 과 web/ 에 있다.",
  "scripts": {
    "setup": "npm --prefix pipeline install && npm --prefix web install",
    "content": "npm --prefix pipeline run build && npm --prefix pipeline run verify",
    "dev": "npm --prefix web run dev",
    "build": "npm run content && npm --prefix web run build",
    "preview": "npm --prefix pipeline run preview",
    "check-dns": "node infra/check-dns.mjs",
    "typecheck": "npm --prefix web run typecheck"
  }
}
