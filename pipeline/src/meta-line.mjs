/**
 * 설명으로 쓰면 안 되는 '라벨: 값' 꼴의 문단을 가려낸다.
 *
 * 노트는 제목 밑에 메타 정보를 한 줄 적는 관례를 자주 쓴다.
 *
 *   기간: 2026.05 ~ 진행중 관련 기술: Next.js, React, TypeScript
 *   기준: gstack v1.77.0.0 (e76f65a8) 관련 노트: gstack MOC
 *
 * 설명은 검색 결과와 링크 미리보기에 그대로 나가므로 이런 줄이 잡히면 안 된다.
 *
 * 처음에는 라벨 이름을 목록으로 갖고 있었는데(기간·관련 기술·역할…), 그 목록은
 * 프로젝트 노트만 보고 만든 것이라 지식 노트의 '기준:' 을 놓쳤다. 새 노트가
 * 새 라벨을 쓸 때마다 목록이 낡는다. 이름 대신 생김새로 가른다.
 *
 * 짧은 말머리 + 콜론 + 값. 사람이 읽을 문장은 이렇게 시작하지 않는다.
 * 드물게 'Docker: 컨테이너 기술이다' 같은 정의문이 걸릴 수 있지만, 그때는
 * 그다음 문단이 설명이 될 뿐 페이지가 망가지지는 않는다.
 */
export const META_LINE = /^[\p{L}\p{N}][\p{L}\p{N} ._/-]{0,14}\s*[:：]\s*\S/u
