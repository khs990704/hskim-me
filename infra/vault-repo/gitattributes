# Vault 저장소용 .gitattributes
# 경로: Vault 루트의 .gitattributes 로 복사
#
# Windows 에서 작성하고 Linux(CI)에서 읽으므로 저장소 안에서는 LF 로 통일한다.
* text=auto eol=lf

# 바이너리 첨부는 변환하지 않는다
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
*.mp3 binary
*.mp4 binary
